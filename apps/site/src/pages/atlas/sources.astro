---
// /atlas/sources — Atlas Sources: the world-data provenance manifold. This is a
// DISTINCT page from the Study's /sources (per §5.2: "≠ Study /sources") — that
// one is the feed intake registry; this one is the world-data provenance layer.
// STUB ONLY for now; the real provenance surface ships with its owning phase.
import AtlasShell from "../../layouts/AtlasShell.astro";
import SurfaceStub from "../../components/atlas/SurfaceStub.astro";
---

<AtlasShell title="khazana atlas — sources" active="sources" crumbs={["sources"]}>
  <SurfaceStub
    heading="Atlas Sources"
    lead="The provenance of the world's data — which dataset, which authority, which vintage, which license backs every indicator and event on the Atlas."
    note="Stub — the world-data provenance surface ships with its owning Atlas phase. Distinct from the Study's /sources feed registry."
  />
</AtlasShell>
