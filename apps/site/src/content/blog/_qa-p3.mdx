---
title: "QA P3: StateMachine, LayerStack, Checklist, GanttStrip, RouteMap + DataTable Totals Under Stress"
format: teardown
channels: ["tech", "science"]
summary: "A non-shipping QA harness that exercises the five new P3 Reads components — StateMachine and LayerStack (teardown/primer), Checklist and GanttStrip (build-log/dispatch), and RouteMap (chronicle/dispatch) — plus the new summable DataTable footer, with realistic props and deliberately stressful edge cases to surface overflow, wrapping, and sizing bugs at 360px, in both motion modes, and with JS off."
publishedAt: 2026-07-01T12:00:00.000Z
sources:
  - title: "Transmission Control Protocol (Wikipedia)"
    url: "https://en.wikipedia.org/wiki/Transmission_Control_Protocol"
draft: true
---

import {
  StateMachine,
  LayerStack,
  Checklist,
  GanttStrip,
  RouteMap,
  DataTable,
} from "../../components/mdx";

This is a QA harness for the **P3 wave**. Each new component appears first with realistic props, then, where relevant, stressed with long labels, many items, and narrow-viewport edge cases so wrapping and reflow can be judged at 360px, in both motion modes, and with JS off.

## 1. StateMachine — sequence mode (realistic) + free mode wide grid (mobile-stress)

<StateMachine
  client:visible
  caption="The TCP three-way handshake: a token walked through the connection states."
  start="closed"
  states={[
    { id: "closed", label: "CLOSED", x: 0, y: 0 },
    { id: "syn-sent", label: "SYN-SENT", x: 1, y: -1 },
    { id: "syn-rcvd", label: "SYN-RCVD", x: 1, y: 1 },
    { id: "established", label: "ESTABLISHED", x: 2, y: 0 },
  ]}
  transitions={[
    { from: "closed", to: "syn-sent", on: "send SYN" },
    { from: "syn-sent", to: "established", on: "recv SYN-ACK / send ACK" },
    { from: "closed", to: "syn-rcvd", on: "recv SYN / send SYN-ACK" },
    { from: "syn-rcvd", to: "established", on: "recv ACK" },
    { from: "established", to: "closed", on: "recv FIN" },
  ]}
  sequence={["closed>syn-sent", "syn-sent>established"]}
/>

<StateMachine
  client:visible
  caption="A tiny recursive-descent expression parser (free mode — click a lit edge)."
  start="start"
  states={[
    { id: "start", label: "Awaiting first token of the expression", x: 0, y: 0 },
    { id: "num", label: "Parsed a numeric literal", x: 1.6, y: -1 },
    { id: "op", label: "Expecting a binary operator", x: 1.6, y: 1 },
    { id: "done", label: "Complete expression accepted", x: 3.2, y: 0 },
  ]}
  transitions={[
    { from: "start", to: "num", on: "digit" },
    { from: "num", to: "op", on: "whitespace" },
    { from: "op", to: "num", on: "+ or -" },
    { from: "num", to: "done", on: "EOF" },
  ]}
/>

## 2. LayerStack — TCP/IP model (realistic) + long-note GPU pipeline (mobile-stress)

<LayerStack
  client:visible
  caption="The four layers of the TCP/IP model, each expandable to its role."
  layers={[
    { label: "Application", note: "The protocols the user actually speaks — HTTP, DNS, SMTP — carrying the data a person or program cares about.", detail: "L7. A browser, a mail client, a resolver all live here." },
    { label: "Transport", note: "End-to-end delivery: the ordered, reliable TCP stream, or fire-and-forget UDP datagrams.", detail: "L4. Ports, segments, the three-way handshake, flow control." },
    { label: "Internet", note: "Addressing and routing across independent networks — IP moves a packet hop to hop toward its destination.", detail: "L3. IP addresses, routing tables, fragmentation, TTL." },
    { label: "Link", note: "The wire, or the air: framing bits onto a single physical segment between adjacent nodes.", detail: "L2/L1. Ethernet, Wi-Fi, MAC addresses, the physical medium." },
  ]}
/>

<LayerStack
  client:visible
  caption="A GPU render pipeline, top to bottom (long notes stress-test 360px wrapping)."
  layers={[
    { label: "Vertex specification", note: "The application binds vertex buffers and attribute layouts describing every position, normal, and texture coordinate that will enter the pipeline this draw call." },
    { label: "Vertex shader", note: "Each vertex is transformed from model space into clip space; per-vertex data is computed and passed down as interpolated varyings." },
    { label: "Rasterization", note: "Clip-space primitives are clipped, projected to the viewport, and broken into fragments — one candidate pixel per covered sample." },
    { label: "Fragment shader", note: "Every fragment is shaded: texture lookups, lighting, and material math produce a candidate color and depth for that pixel." },
    { label: "Per-sample operations", note: "Depth and stencil tests, blending, and the framebuffer write decide which fragments survive to become visible pixels." },
  ]}
/>

## 3. Checklist — realistic build steps + long-title/long-label (mobile-stress)

<Checklist
  client:visible
  title="Reproduce the desk sensor node"
  caption="one evening, start to first reading"
  items={[
    { label: "Flash MicroPython to the ESP32", note: "hold BOOT while connecting", href: "https://micropython.org/download/ESP32_GENERIC/" },
    { label: "Wire the BME280 to I²C (SDA=21, SCL=22)" },
    { label: "Solder the 4-pin header", note: "flux helps on the tiny pads" },
    { label: "Copy main.py and set the Wi-Fi creds" },
    { label: "Confirm a reading appears in the serial console" },
  ]}
/>

<Checklist
  client:visible
  title="A deliberately long title that must wrap gracefully at 360px width without overflow"
  items={[
    { label: "A very long step label that runs well past a single line so we can confirm the row wraps, the checkbox stays top-aligned, and nothing pushes past the 360px viewport edge", note: "and a secondary note line that also wraps on its own full-width row beneath the label", href: "https://example.com/some/rather/long/reference/url/that/should/not/blow/out/the/layout" },
    { label: "Short one" },
  ]}
/>

## 4. GanttStrip — multi-day build phases (realistic) + dense same-day (mobile-stress)

<GanttStrip
  client:visible
  unit="day"
  caption="how long each phase of the build actually took"
  tasks={[
    { label: "Design + BOM", start: 0, end: 3, note: "most of the time was sourcing the right sensor" },
    { label: "PCB fab wait", start: 3, end: 17, note: "JLCPCB, cheapest shipping" },
    { label: "Assembly", start: 17, end: 19, note: "reflow + hand-solder the headers" },
    { label: "Firmware", start: 19, end: 24 },
    { label: "Enclosure print", start: 22, end: 25, note: "overlapped with firmware" },
  ]}
/>

<GanttStrip
  client:visible
  unit="hr"
  caption="a dense same-day session — forces the sub-520px fallback list"
  tasks={[
    { label: "Bring-up and a very long phase name that would collide with its bar in the SVG", start: 0, end: 1, note: "verifies the mobile list-promote path" },
    { label: "Debug", start: 1, end: 1, note: "zero-duration phase → still gets a visible min-width bar" },
    { label: "Soak test", start: 1, end: 9 },
  ]}
/>

## 5. RouteMap — historical march (realistic) + trans-oceanic great circles (mobile-stress)

<RouteMap
  client:visible
  routes={[
    { from: [2.35, 48.85], to: [37.62, 55.75], label: "Paris → Moscow (advance)", kind: "march" },
    { from: [37.62, 55.75], to: [2.35, 48.85], label: "Moscow → Paris (retreat)", kind: "path" },
  ]}
  points={[
    { at: [37.62, 55.75], label: "Moscow (burned)" },
    { at: [31.99, 54.78], label: "Smolensk" },
    { at: [23.35, 53.13], label: "Vilnius" },
  ]}
  values={{ RUS: 100, POL: 55, DEU: 40, FRA: 25 }}
  labels={{ RUS: "Russian Empire", FRA: "France" }}
  caption="1812: la Grande Armée's march to Moscow and the long retreat home."
/>

<RouteMap
  client:visible
  routes={[
    { from: [-74.0, 40.71], to: [139.69, 35.69], label: "New York → Tokyo (a very long trans-Pacific great-circle arc)", kind: "arc" },
    { from: [-0.13, 51.51], to: [151.21, -33.87], label: "London → Sydney", kind: "path" },
    { from: [-58.38, -34.6], to: [18.42, -33.92], label: "Buenos Aires → Cape Town", kind: "march" },
    { from: [-122.42, 37.77], to: [4.90, 52.37], label: "San Francisco → Amsterdam", kind: "arc" },
  ]}
  points={[
    { at: [-149.86, 61.22], label: "Anchorage — near the polar great-circle apex" },
  ]}
  caption="Great-circle routes bow poleward — the shortest path is not the straight line on a flat map."
/>

## 6. DataTable with `total` — a bill of materials (summable amber footer)

<DataTable
  client:visible
  caption="Bill of materials for the sensor node — the Cost column sums in the footer"
  total="cost"
  columns={[
    { key: "part", label: "Part", type: "string" },
    { key: "qty", label: "Qty", type: "number", align: "right" },
    { key: "cost", label: "Cost ($)", type: "number", align: "right" },
  ]}
  rows={[
    { part: "ESP32 dev board", qty: 1, cost: 6 },
    { part: "BME280 sensor", qty: 1, cost: 4 },
    { part: "0.96\" OLED", qty: 1, cost: 3 },
    { part: "Perfboard + headers", qty: 1, cost: 2 },
    { part: "3D-printed enclosure", qty: 1, cost: 1 },
  ]}
/>

The footer should read **Total 16** in amber, right-aligned under the Cost column.

## 7. Added stress cases (QA agent) — many-state SM, right-edge point, wide table

<StateMachine
  client:visible
  caption="Many-state stress: a wide 7-state TCP connection lifecycle (must fit / fall back at 360)."
  start="listen"
  states={[
    { id: "listen", label: "LISTEN", x: 0, y: 0 },
    { id: "syn-rcvd", label: "SYN-RCVD", x: 1, y: -1 },
    { id: "syn-sent", label: "SYN-SENT", x: 1, y: 1 },
    { id: "established", label: "ESTABLISHED", x: 2, y: 0 },
    { id: "fin-wait", label: "FIN-WAIT-1", x: 3, y: -1 },
    { id: "close-wait", label: "CLOSE-WAIT", x: 3, y: 1 },
    { id: "closed", label: "CLOSED", x: 4, y: 0 },
  ]}
  transitions={[
    { from: "listen", to: "syn-rcvd", on: "recv SYN" },
    { from: "listen", to: "syn-sent", on: "send SYN" },
    { from: "syn-rcvd", to: "established", on: "recv ACK" },
    { from: "syn-sent", to: "established", on: "recv SYN-ACK" },
    { from: "established", to: "fin-wait", on: "close" },
    { from: "established", to: "close-wait", on: "recv FIN" },
    { from: "fin-wait", to: "closed", on: "recv FIN-ACK" },
    { from: "close-wait", to: "closed", on: "send FIN" },
  ]}
/>

<RouteMap
  client:visible
  routes={[
    { from: [151.21, -33.87], to: [174.76, -36.85], label: "Sydney → Auckland", kind: "arc" },
  ]}
  points={[
    { at: [178.44, -18.14], label: "Suva, Fiji — a point hard against the right map edge (≈178°E)" },
    { at: [-175.2, -21.18], label: "Nukuʻalofa — just past the antimeridian" },
  ]}
  caption="Right-edge stress: a point at ≈178°E must stay on-screen, its label not clipped past the map."
/>

<DataTable
  client:visible
  caption="Wide-cell stress: long part descriptions + a summed cost footer (scrolls inside .dt-scroll)."
  total="cost"
  columns={[
    { key: "part", label: "Component (long description)", type: "string" },
    { key: "supplier", label: "Supplier", type: "string" },
    { key: "qty", label: "Qty", type: "number", align: "right" },
    { key: "cost", label: "Cost ($)", type: "number", align: "right" },
  ]}
  rows={[
    { part: "ESP32-WROOM-32 development board with USB-C, CP2102 bridge, and pre-soldered headers", supplier: "AliExpress seller with an extremely long store name that will not wrap", qty: 2, cost: 12 },
    { part: "Bosch BME280 temperature / humidity / barometric pressure sensor breakout", supplier: "Adafruit", qty: 1, cost: 9 },
    { part: "0.96-inch SSD1306 I²C monochrome OLED display module", supplier: "Amazon", qty: 1, cost: 5 },
    { part: "Assorted perfboard, headers, and 22AWG hookup wire kit", supplier: "local", qty: 1, cost: 7 },
  ]}
/>

The wide-table footer should read **Total 33** in amber under the Cost column.
